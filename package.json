{
  "name": "dops-website",
  "version": "1.0.0",
  "private": true,
  "description": "DOPS Parfume website prototype, split from single index.html into deploy-ready directory.",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3000",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 3000",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.0",
    "vite": "^5.4.0"
  }
}